[build]
  # 设置基础目录为前端项目所在位置
  base = "frontend/videco"
  
  # 构建命令
  command = "npm run build"
  
  # 构建输出目录（Vite 默认输出到 dist）
  publish = "dist"

# 单页应用（SPA）重定向配置，确保路由正常工作
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 设置 Node.js 版本
[build.environment]
  NODE_VERSION = "18"
